version: '3.9'

services:
  api:
    build:
      context: .
      target: ${TARGET:-prod}
    ports:
      - '${API_PORT:-3000}:3000'
    environment:
      - NODE_ENV=${NODE_ENV:-production}
    volumes:
      - ${VOLUMES:-none}
# Remove volumes section since we're not using local MongoDB
